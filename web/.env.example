# Discord Application ID (same value as DISCORD_CLIENT_ID in the repo root .env).
# Required at build time — a missing value causes a blank Activity screen in Discord.
VITE_DISCORD_CLIENT_ID=replace-with-your-application-id
